name: Build and Release V8 Data

on:
  push:
    branches: [ "main" ]
    paths:
      - 'assets/final/**'
      - 'src/builder.py'
      - '.github/workflows/release.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Build Full Database
        run: |
          mkdir -p dist
          python src/builder.py --input assets/final/full --output dist/liumo_v8_full.db
          gzip -c dist/liumo_v8_full.db > dist/liumo_v8_full.db.gz
          
      - name: Build Lite Database
        run: |
          python src/builder.py --input assets/final/lite --output dist/liumo_v8_lite.db
          gzip -c dist/liumo_v8_lite.db > dist/liumo_v8_lite.db.gz
          
      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT
        
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v8-data-${{ steps.date.outputs.date }}
          name: V8 Data Update ${{ steps.date.outputs.date }}
          files: |
            dist/liumo_v8_full.db.gz
            dist/liumo_v8_lite.db.gz
          draft: false
          prerelease: false
